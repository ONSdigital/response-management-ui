<% if (!surveyid.nil?) and (surveyid.match('^[0-9]*$'))  %>
  {
    "surveyid": "<%= surveyid %>",
    "surveyName": "string",
    "description": "string"
  }
<% else %>
[
  {
    "code": 0,
    "message": "string",
    "fields": "string"
  }
]
<% end %>
