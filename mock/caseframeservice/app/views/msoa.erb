<% if (!msoa_code.nil?) and (msoa_code.match('^E[0-9]{8}$'))  %>
[
  {"msoa_code":"<%= msoa_code %>","msoa_name":"Hartlepool 001"}
]
<% else %>
[
  {
  "code": 0,
  "message": "string",
  "fields": "string"
  }
]
<% end %>
