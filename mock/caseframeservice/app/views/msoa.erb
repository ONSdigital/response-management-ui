<% if (!msoaCode.nil?) and (msoaCode.match('^E[0-9]{8}$'))  %>
[
  {"msoaCode":"<%= msoaCode %>","msoaName":"Hartlepool 001"}
]
<% else %>
[
  {
  "code": 0,
  "message": "string",
  "fields": "string"
  }
]
<% end %>
