<% if (!casetypeid.nil?) and (casetypeid.match('^[0-9]*$'))  %>
[
  {
  "casetypeid": "<%= casetypeid %>",
  "casetypeName": "string",
  "description": "string",
  "actionplanid": 0,
  "formtype": "string"
  }
]
<% else %>
[
  {
    "code": 0,
    "message": "string",
    "fields": "string"
  }
]
<% end %>
