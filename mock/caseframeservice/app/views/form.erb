<% if (!formtype.nil?) and (formtype.match('^[0-9]*$'))  %>
[
  {
    "formtype": "<%= formtype %>",
    "description": "string"
  }
]
<% else %>
[
  {
    "code": 0,
    "message": "string",
    "fields": "string"
  }
]
<% end %>
